{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "studio": "npx prisma studio",
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate",
    "dev": "npm run migrate && npm run generate && npm run studio & nodemon --no-warnings index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemon": "^3.1.7",
    "pg": "^8.13.1",
    "prisma": "^5.22.0"
  }
}
